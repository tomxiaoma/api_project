AlbumListQuery:
  dec: "成长册列表查询（书架页）"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query": "query AlbumListQuery($studentId: ID!) { albums(studentId: $studentId) { __typename termId avatarUrl backgroundImg coverImg coverImgV1 lockedCoverImgV1 currentStage href isShown isCurrent isLocked name termInfo pageNumber }}",
        "variables":"{ \"studentId\": \"92475\" }"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
