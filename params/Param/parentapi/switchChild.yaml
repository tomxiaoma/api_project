switchChild:
  dec: "切换宝贝"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"mutation switchChild($childId: ID!, $studentId: ID) { switchChild(input: {childId: $childId, studentId: $studentId}) { __typename res }}",
        "variables":"{ \"childId\": \"253136\", \"studentId\": \"294398\" }"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
