VerifyPasswordMutation:
  dec: "修改手机号通过密码校验身份"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"mutation VerifyPasswordMutation($input: VerifyPasswordInput!) { verifyPassword(input: $input) { __typename code message }}",
        "variables":"{ \"input\": { \"password\": \"123123\" } }"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
