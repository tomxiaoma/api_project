CheckPhoneAndSendSmsMutation:
  dec: "检测手机号码是否可用"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"mutation CheckPhoneAndSendSmsMutation($input: CheckPhoneAndSendSmsInput!) { checkPhoneAndSendSms(input: $input) { __typename code message }}",
        "variables":"{ \"input\": { \"newPhone\": \"13000000000\" } }"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
