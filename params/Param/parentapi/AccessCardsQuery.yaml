AccessCardsQuery:
  dec: "我的考勤卡查询"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"query AccessCardsQuery($studentId:ID!) { accessCards(studentId: $studentId) { number kindergarten{ name } } }",
        "variables":"{ \"studentId\": \"92475\" }"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
