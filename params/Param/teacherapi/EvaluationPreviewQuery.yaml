EvaluationPreviewQuery:
  dec: "根据ID查看评价预览"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"query EvaluationPreviewQuery($evaluationId:ID!){ evaluation(evaluationId:$evaluationId){ id title evaluationStatistics{ unreachedCount student{ id avatarUrl name } unreachedEvaluations{ id star evaluationAttribute{ id firstDomain secondDomain title } } } } }",
        "variables": "{\"evaluationId\":15}"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
