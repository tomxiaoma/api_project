MessageListQuery:
  dec: "消息列表查询"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"query MessageListQuery($first:Int $after:String $last:Int $before:String $clazzId:ID $startId:ID){ messages(first:$first after:$after last:$last before:$before clazzId:$clazzId startingId:$startId){ pageInfo{ hasNextPage } edges{ cursor}pageInfo{endCursor} } }",
        "variables": "{\"id\":456}"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
