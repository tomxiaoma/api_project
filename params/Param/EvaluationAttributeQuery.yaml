EvaluationAttributeQuery:
  dec: "根据评价属性ID查询评价信息"
  parameters:
    -
      url: /rest/graphql
      data: {
        "query":"query EvaluationAttributeQuery($attributeId:ID!){ evaluationInfo(evaluationAttributeId:$attributeId) { evaluationAttribute{evaluationCategoryId firstDomain isFinished id secondDomain title}evaluationValues {star studentName }} }",
        "variables": "{\"attributeId\":135}"
      }

      header: {
                 "Content-Type": "application/x-www-form-urlencoded"
              }
